spring:
  config:
    active:
      on-profile: unittest
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
    username: root
    password: mypwd
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false

cors:
  allowedOrigins: '*'
